import React from 'react'

import Navbar from "../common/Navbar"

const Booking = (props) => {

    document.title = 'Bookings | Vendor'
    
    return (
        <React.Fragment>
            <Navbar />
            <div className="container">
            </div>
        </React.Fragment>
    )
}

export default Booking